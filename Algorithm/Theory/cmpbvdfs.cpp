#include <bits/stdc++.h>

void dfs(int y, int x) {
    visited[y][x] 
}

using namespace std;
int N, M;
int main() {

    cin >> N >> M;

    int inputArray[N][M];
    int visited[N][M];
    int temp;
    cout << N << " " << M << endl;

    for (int i=0; i<N; i++) {
        for (int j=0; j<M; j++) {
            cin >> temp
            inputArray[i][j] = temp;
        }
    }

}